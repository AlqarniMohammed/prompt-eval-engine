# Copy to .env (gitignored). API-key transport ONLY — never subscription auth.
ANTHROPIC_API_KEY=YOUR_API_KEY
# Only needed for a cross-family judge (uv sync --extra cross-judge):
# OPENAI_API_KEY=YOUR_API_KEY
